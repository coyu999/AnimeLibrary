<UserControl x:Class="AnimeLibrary.View.UserControls.SearchTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnimeLibrary.View.UserControls"
             mc:Ignorable="d" 
             Height="30" Width="150">
    <Border CornerRadius="15" BorderThickness="1" BorderBrush="#CED5D8" Background="Transparent">
        <Grid>
            <TextBox Name="txtInput" VerticalContentAlignment="Center" FontSize="16"
                Background="Transparent" Foreground="White"
                TextChanged="txtInput_TextChanged"  BorderThickness="0"
                Margin="12,0,0,2" CaretBrush="#CED5D8"/>
            <TextBlock Name="tbPlaceHolder" Text="{Binding PlaceHolder, UpdateSourceTrigger=PropertyChanged}" FontSize="16" 
                FontWeight="Bold" Foreground="DarkGray"
                VerticalAlignment="Center" Margin="12,0,0,2" Panel.ZIndex="-1"
                HorizontalAlignment="Left"/>
        </Grid>
    </Border>
</UserControl>
